# 面向多模态与非完备医学的信息论工具箱

本路线不把“最大化互信息”当成统一答案。先从研究问题出发，再选择信息量、散度、表示约束或决策指标；如果普通概率模型、监督损失或几何误差已经足够，就不强行加入信息论目标。

## 1. 五个学习模块

| 模块 | 核心概念 | 主要解决的问题 | 对应学习日 |
|---|---|---|---|
| A. 不确定性与预测 | 自信息、熵、条件熵、交叉熵、NLL、预测熵 | 模型有多不确定；看到某模态后还剩多少不确定性 | Day 015–018、036–040 |
| B. 分布与依赖 | KL、JS、互信息、条件互信息、数据处理不等式 | 两个域/模态是否不同；表示保留或丢失了什么 | Day 019–021、043–049 |
| C. 压缩与表示 | 率失真、MDL、信息瓶颈、VIB、对比目标 | 在有限带宽/容量下保留任务相关信息 | Day 025–035 |
| D. 多变量与非完备 | 总相关、PID、冗余、独有、协同、MCAR/MAR/MNAR | 哪个模态提供独有信息；缺失是否带有选择偏差 | Day 036–042、053–056 |
| E. 医学决策可靠性与隐私 | 校准、Brier/NLL、选择性预测、风险—覆盖率、主动模态获取、差分隐私 | 何时应相信/拒绝预测；下一项检查是否值得获取；如何约束患者级泄漏 | Day 053–063 |

## 2. 从问题选择工具

| 研究问题 | 首选工具 | 同时必须报告 | 不应直接下的结论 |
|---|---|---|---|
| 预测分布是否过于自信 | NLL、Brier、ECE、可靠性图 | AUROC/AUPRC、分组校准、置信区间 | “熵高所以模型一定错” |
| 新模态是否降低标签不确定性 | 条件熵、条件互信息、消融 | 单/多模态任务指标、缺失子集表现 | “MI 高所以有因果价值” |
| 两家医院的数据是否发生偏移 | KL/JS 或其他两样本度量 | 特征/标签/缺失率分层、外部验证 | “散度小所以临床等价” |
| 表示是否保留任务信息并去除噪声 | 信息瓶颈、率失真、充分性检验 | 下游性能、压缩率、校准、鲁棒性 | “维度更低就是信息瓶颈” |
| 多模态是冗余还是互补 | PID/总相关作为探索分析 | 全子集消融、置信区间、估计器敏感性 | “一个 PID 数值就是唯一真相” |
| 检查缺失时如何预测 | 缺失掩码、条件模型、集合式融合、蒸馏/生成 | 按缺失模式分层的性能与校准 | “补全图像逼真所以诊断可靠” |
| 下一项检查是否值得做 | 期望信息收益或价值信息 + 获取成本 | 风险、费用、时间、患者负担和停止规则 | “信息最多的检查一定最合适” |
| 医学图像如何跨模态配准 | MI/NMI、联合直方图 | 几何误差、重采样设置、失败病例 | “MI 上升就保证解剖对齐” |
| 多中心共享时如何限制单个患者信息泄漏 | 差分隐私、敏感度、隐私预算与组合 | 威胁模型、ε/δ、任务性能代价 | “匿名化或低 MI 就保证隐私” |

## 3. 非完备多模态的最低实验协议

任何涉及缺失模态的项目至少要完成：

1. 明确可用模态集合和缺失指示变量 `R`。
2. 区分训练时缺失、测试时缺失和真实采集流程缺失。
3. 讨论 MCAR、MAR、MNAR 中哪个假设可能成立；不能从数据本身证明时要明确写成假设。
4. 按每种模态子集分别报告样本量、任务性能和校准，不只报告随机 dropout 平均值。
5. 比较至少一个简单基线：零填充/均值填充 + mask、只用稳定单模态、可用模态集合池化。
6. 对缺失率、缺失模式、机构、设备和人群做压力测试。
7. 区分“预测目标”“重建缺失模态”和“决定是否获取新模态”三类任务。

## 4. 医学可靠性的最低实验协议

- 分类至少报告 AUROC 或 AUPRC、NLL/Brier、可靠性图和一个校准摘要；类别极不平衡时不能只报 accuracy。
- 分割/重建除 Dice、Hausdorff、RMSE 等任务指标外，要检查不确定性是否集中在错误区域。
- 选择性预测要画风险—覆盖率曲线：拒绝更多病例是否真的降低剩余病例风险。
- 校准参数只能用独立验证集拟合，不能在测试集调 temperature 或阈值。
- 患者级、机构级和时间级划分优先于随机切片划分，防止同一患者信息泄漏。
- 涉及数据共享或模型发布时，区分工程数据泄漏、成员/重识别风险和差分隐私保证；若使用差分隐私，必须报告威胁模型、ε/δ 与效用代价。
- 信息量、散度或估计下界只是证据之一，不能替代外部验证和临床效用分析。

## 5. 使用边界与常见误区

1. **MI 不是因果性。** 高互信息可能来自患者身份、扫描设备、报告模板或标签泄漏。
2. **微分熵可为负且依赖坐标尺度。** 不要把连续变量的微分熵当作离散熵直接比较。
3. **KL 不对称且可能无穷大。** 用作域差异时必须说明方向、平滑和支持集。
4. **InfoNCE 是训练目标/下界，不等同于准确测出真实 MI。** batch size、负样本构造和 false negatives 会影响结果。
5. **PID 没有唯一公认分解。** 冗余/协同结论要报告定义、估计器和敏感性分析。
6. **高预测熵不等同于高临床风险。** 需要校准、代价和错误类型共同决定是否拒绝预测。
7. **缺失值本身可能有信息。** 若医生基于病情选择检查，缺失模式可能与标签相关，随机遮挡不能完整模拟现实。
8. **信息最多不等于最值得获取。** 主动模态选择还要考虑费用、侵入性、时间和患者负担。
9. **匿名化、低相关性或低 MI 不等于隐私保证。** 差分隐私需要显式威胁模型、ε/δ 和效用审计。

## 6. 阶段交付

- **Day 28**：信息度量计算与有限样本审计 Notebook。
- **Day 42**：含 MCAR/MAR/MNAR、模态子集消融、PID 探索和主动获取的设计稿。
- **Day 56**：医学配准/缺失模态/校准三选一可复现实验。
- **Day 63**：非完备医学多模态综合基准与 5 分钟答辩。
- **Day 168**：在三维机器人结业项目中选择至少两项真正有任务价值的信息论工具，并用工程/医学指标验证。
